NAME MEMORY

PUBLIC MEM_INIT
PUBLIC CLEAR_PACKET_BUFFER

MEM_INIT:

    MOV R0, #70H
    MOV R1, #20H

CLEAR_LOOP:

    MOV @R0, #00H
    INC R0

    DJNZ R1, CLEAR_LOOP

    RET

CLEAR_PACKET_BUFFER:
    ACALL MEM_INIT
    RET
