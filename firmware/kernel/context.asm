NAME CONTEXT

PUBLIC SAVE_CONTEXT
PUBLIC RESTORE_CONTEXT

SAVE_CONTEXT:

    PUSH ACC
    PUSH B
    PUSH DPL
    PUSH DPH
    PUSH PSW

    RET

RESTORE_CONTEXT:

    POP PSW
    POP DPH
    POP DPL
    POP B
    POP ACC

    RET
