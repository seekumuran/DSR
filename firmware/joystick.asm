; ==========================================
; FAKE ADC VALUES
; Replace later with ADC0804 reads
; ==========================================

; LEFT JOYSTICK X

READ_LX:

    MOV A, #080H
    RET


; LEFT JOYSTICK Y

READ_LY:

    MOV A, #080H
    RET


; RIGHT JOYSTICK X

READ_RX:

    MOV A, #080H
    RET


; RIGHT JOYSTICK Y

READ_RY:

    MOV A, #080H
    RET
